# Filter and Theme Color-Only Fix

Updated files:
- src/pages/Home.tsx
- src/index.css

Changes:
- Applied the same compact search/filter sizing across tabs and report filter areas.
- Removed the hard inline filter grid so CSS controls the filter panel consistently.
- Kept the same background image geometry for dark and light themes.
- Light/Dark theme now changes color/tint only; logos and shapes remain in the same location.
