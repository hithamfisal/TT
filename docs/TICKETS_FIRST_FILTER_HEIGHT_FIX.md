# Tickets First and Filter Height Fit

Updated files:
- src/pages/Home.tsx
- src/index.css

Changes:
- Tickets tab is now the first tab.
- Tickets tab is now the default active dashboard tab.
- Return/reset tab behavior now returns to Tickets.
- Search/filter bar height reduced further across tabs.
- Hero panel no longer forces extra empty height behind the filter area.
