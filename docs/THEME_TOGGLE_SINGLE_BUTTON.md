# Theme Toggle Single Button Update

Changed the theme selector from two separate buttons to one toggle button.

Behavior:

- Initial/current dark mode caption: `Dark Theme`
- After pressing, dashboard switches to light mode and caption becomes: `Light Theme`
- Press again to return to dark mode.

Updated file:

- `src/pages/Home.tsx`
